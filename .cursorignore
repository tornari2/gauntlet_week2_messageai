# Environment files with sensitive API keys
.env
.env.*
*.env

# Firebase configuration files
firebase-config.json
.firebase/

# Any files containing secrets
*secret*
*credential*
*key.json

# Node modules (already in .gitignore but good to exclude from AI)
node_modules/

# Build outputs
dist/
build/
.expo/
